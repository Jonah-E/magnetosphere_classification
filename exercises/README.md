# Magnetospheric Classification Exercises

The following folder contains exercise notebooks for the magnetospheric classification lecture.
The subfolder `utils` contains necessary utility functions used in the notebooks.

## Prerequisites

Python environment (Python 3.12 recommended) with modules from the `requirements.txt` file located in the repositories root folder installed.

```
pip install -r requirements.txt
```
